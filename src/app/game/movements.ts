//Handles attacking squares, etc by piece

import { ChessState, Piece, Square } from "./models";

/**
 * Return the squares currently attacked by the given piece.
 */
export function attackedSquares(piece: Piece, state: ChessState): Square[] {
    throw 'not done';
}